package com.example.chatty.data.enums;

/**
 * Representing the type of recieved message and sent answers. It can be greeting, joke, conversion or notunderstood type.
 */
public enum RequestType {
    JOKE, CONVERSION, GREETING, NOTUNDERSTOOD
}
